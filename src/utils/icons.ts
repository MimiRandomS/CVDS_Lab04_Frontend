import { library } from "@fortawesome/fontawesome-svg-core";
import {
  faUser,
  faList,
  faCircleInfo,
  faCalendar,
  faClock,
} from "@fortawesome/free-solid-svg-icons";

library.add(faUser, faList, faCircleInfo, faCalendar, faClock);
